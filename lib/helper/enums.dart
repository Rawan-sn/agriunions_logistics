enum RequestType { get, post, put, delete }
enum ProjectType { production, staging }
enum ScreenSizes { extraLarge, large, medium, small, extraSmall, twoExtraSmall }
enum LoadingType { fadingCube, circularProgress, threeBounce }
enum ErrorAssetsType { vehicle,chatImage }
enum UserTypes { service_provider, user, all }
